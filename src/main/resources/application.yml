#端口号
server:
    port: 8080

#数据库链接设置
spring:
    datasource:
        url: jdbc:mysql://192.168.18.203:3306/db_study?useUnicode=true&characterEncoding=utf-8&useSSL=true
        username: root
        password: 123456
        driver-class-name: com.mysql.jdbc.Driver
        type: com.alibaba.druid.pool.DruidDataSource




#整合Mybatis
mybatis:
    #  扫描数据持久对应的实体类 路径 Package级别
    type-aliases-package: com.study.domain
    #  mybatis 对应的 .xml文件路径
    mapper-locations: classpath:mapper/*.xml
#  多层级目录  mapper-locations: classpath:mapper/**/**.xml

